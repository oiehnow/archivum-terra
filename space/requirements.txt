# 서비스 런타임
fastapi>=0.115
uvicorn[standard]>=0.30
itsdangerous>=2.2

# 검색: 벡터/키워드 인덱스 조회만 한다.
# 질의 임베딩은 HF Inference API 에 맡기므로 torch 도 모델도 필요 없다.
lancedb>=0.13
bm25s>=0.2

# 인덱스를 Dataset repo 에서 내려받기
huggingface-hub>=0.25

# 선택 경로: 서버에서 답변을 생성할 때만 쓴다 (Puter 경로에서는 미사용)
authlib>=1.3
httpx>=0.27
google-genai>=1.0
